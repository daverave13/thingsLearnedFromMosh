// Square brackets are called array literals
let selectedColors = ['red', 'blue']; 

//Array length is dynamic, you can just add one like this
selectedColors[2] = 23

//Array data types are also dynamic, notice the number instead of a string

console.log(selectedColors);
